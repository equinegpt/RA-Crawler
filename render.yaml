services:
  - type: web
    name: ra-crawler
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash scripts/start.sh
    autoDeploy: true
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
